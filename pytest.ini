[pytest]
asyncio_mode = auto
# examples/ holds live-vehicle scripts that need real credentials and network.
# Without this they are collected by a bare `pytest` and error at import.
testpaths = tests
